name: my_home_food_os
description: MY HOME FOOD OS - offline dynamic family food, budget, pantry, house-care and voice copilot.
publish_to: none
version: 2.0.0+2
environment:
  sdk: ">=3.9.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  speech_to_text: ^7.5.0
  flutter_tts: ^4.2.5
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
